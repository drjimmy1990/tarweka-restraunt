# Server Configuration
PORT=4000
NODE_ENV=development

# Supabase Configuration
# The "Project URL"
SUPABASE_URL=https://your-project-id.supabase.co

# The "service_role" secret (Use this for the Backend ONLY. It bypasses RLS)
# WARNING: Never share this key or put it in the Frontend.
SUPABASE_SERVICE_KEY=your-service-role-key-here

# The "anon" public key (Used for client-side auth verification)
SUPABASE_ANON_KEY=your-anon-key-here

# Security
# A secret key for n8n to talk to this API (Make up a strong password)
N8N_API_KEY=my_super_secret_n8n_password
